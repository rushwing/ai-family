---
bug_id: BUG-030
title: "Agent 侧 LLM 网关纪律执行：egress 强制 / fail-closed / per-user 预算"
bug_type: req_bug
status: open
severity: high
owner: unassigned
linked_req: REQ-003
regression_tc: []
blocked_reason: ""
---

## 现象
BUG-003 的网关侧（厂商 key 隔离 + secret 扫描 + egress 拓扑声明）已在 REQ-002 闭环；但**执行点在 Agent 侧**，需 agent-core（REQ-003）存在才能实现/验证：
1. **egress 强制**：agent-core 容器只接 `agent_internal`(internal) 网，可达 litellm:4000 但无外网出口 → 技术上禁直连厂商域名（compose 已声明拓扑，待 agent-core 接入并验证）。
2. **fail-closed**：LiteLLM 不可用时 agent 的 LLM 客户端必须 fail-closed（拒绝/降级而非裸调厂商），需在 agent-core 的 LLM 客户端实现。
3. **per-user 预算**：master key + `/key/generate` 下发 per-user 虚拟 key；预算超限打真实网关返回 429/阻断（TC 需真实计量）。

## 复现步骤 / 评审原文
- 承自 [codex-004][2026-06-14]（见 BUG-003）；REQ-002 网关侧 resolved 后剩余 agent 侧执行项。

## 期望行为
agent-core 容器无外网出口（只达网关）；LiteLLM 宕机 fail-closed；per-user 预算可靠阻断（真实网关验证）。

## 修复方案
随 REQ-003 agent-core 落地：① agent-core 接 `agent_internal` 网 + e2e 验证无法直连厂商；② LLM 客户端 fail-closed 单测/集成；③ LiteLLM per-user 虚拟 key + 预算超限集成测试（打真实网关）。
