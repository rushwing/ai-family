---
bug_id: BUG-003
title: "LiteLLM 网关强制与厂商 key 隔离缺乏验证机制"
bug_type: req_bug
status: resolved
severity: high
owner: unassigned
linked_req: REQ-002
regression_tc: [TC-002-08]
blocked_reason: ""
---

## 现象
ADR-002 把 LiteLLM 当预算硬顶与厂商 key 唯一持有者，但未定义 key 同步失败、预算计数延迟、网关宕机降级、“Agent 不直连”禁令如何验证。若只靠约定，后续 Agent 很容易绕开网关。

## 复现步骤 / 评审原文
- [codex-004][2026-06-14] 加 CI secret 扫描和 egress 限制，禁止 Agent 容器持有厂商 key；定义 LiteLLM 不可用时 fail-closed 还是降级；预算超限 TC 必须打真实网关。

## 期望行为
厂商 key 仅存在于网关；Agent 容器无任何厂商凭证且无法直连厂商；预算超限可靠阻断。

## 根因分析
强制点只在配置约定层，无技术门禁与出网限制。

## 修复方案
1. CI 扫描 Agent 镜像/配置不得含厂商 key；2. egress 白名单仅允许 Agent→LiteLLM，不放行厂商域名（挂靠 02 §2 I4）；3. 明确 LiteLLM 不可用 = fail-closed；4. 预算超限 TC 打真实网关验证。

## scope triage
- [planner-001][2026-06-17] 留 REQ-002：LiteLLM 厂商 key 隔离（master key + per-user 虚拟 key），本地起 LiteLLM 后经 TC-002-08 验证。REQ-002 done 仅余本 bug + 本地栈三 TC。

## 进度
- [generator-001][2026-06-20] 部分验证：本地 OrbStack 栈经 TC-002-08 真验「网关强制 key（无 key→401）+ 厂商 key 仅在网关 env + 三后端档位列出」。

## 修复记录（REQ-002 范围 resolved + agent 侧拆出）
- [generator-001][2026-06-20] 本 bug 跨网关侧（REQ-002）+ Agent 侧（REQ-003 agent-core）。**REQ-002 网关侧已闭**：① 网关强制 key + 厂商 key 仅在网关（TC-002-08 本地真验）；② `tools/check_secrets.py` CI 阻塞门——跟踪文件不得含厂商 key 字面量（修复方案①）；③ egress 白名单**拓扑**在 `compose.dev.yaml` 的 `agent_internal`(internal) 网声明（修复方案②的网关侧）。
  **Agent 侧执行无法在 REQ-002 验证**（agent-core 属 REQ-003、现不存在）：egress 实际强制（Agent 只接 internal 网）、LiteLLM 不可用 fail-closed（调用方行为）、per-user 预算超限（虚拟 key + 真实计量）→ 拆为 **BUG-030（linked REQ-003）**。
  故 BUG-003 → **resolved**（REQ-002 网关侧闭环；agent 侧由 BUG-030 承载）。regression_tc: TC-002-08。

## scope 对齐（round-3，2026-06-20）
BUG-003 范围 = **网关侧**：① 网关强制 key（无 key 401）② 厂商 key 仅在网关 env ③ secret 扫描门 ④ egress 拓扑 ⑤ **三后端 dispatch 路由**（TC-002-08 收敛后真验：各档路由到对应 provider）。真实 completion + per-user 预算/超限 + 缺 user 拒绝属 agent 侧/真实凭证 → BUG-030（REQ-003）。故 BUG-003 维持 resolved，regression_tc=TC-002-08 已与收敛后验收对齐。
