---
req_id: REQ-001
title: "架构设计稿 v1 定稿（含 Codex/Gemini 双评审闭环）"
status: req_review
owner: human-001
priority: P0
phase: M0
scope: design
tc_policy: exempt
exempt_reason: "纯设计交付物，验收以外部评审闭环为准，无可自动化测试对象"
depends_on: []
test_case_ref: []
acceptance: "docs/design/ 七份设计文档与 docs/adr/ 十二份 ADR 通过 Codex 与 Gemini 评审，所有 req_bug 关闭后本 REQ 置 done"
review_round: 0
pending_bugs: []
pr_number: null
---

## 背景

项目启动需求：参考 `reference.html`（GCP 企业多智能体方案）做家庭 AI Agent 集群的 Top-Down 架构设计，
production 级选型、以 GoalAgent 为垂直切片起点、设计先行不写代码。

## 需求描述

交付 v1 设计稿全集：

1. `README.md` + `GLOSSARY.md`（渐进式披露入口 + 术语权威表）
2. `harness/` 工程方法标准 ×4 + 首批 REQ ×3
3. `docs/adr/ADR-001 ~ ADR-012`（每个含 Options pros/cons、Trade-off、Revisit Trigger）
4. `docs/design/00 ~ 06` 七份 HTML 设计文档（三色笔记风格，内嵌 SVG：L0 全景图、
   六层架构图、组件框图、部署图、内外网穿透安全图、数据流图 ×2、rollout 时间线）

## 非目标

- 不写任何应用代码；不部署服务；不改 goal-agent / travel_agent 仓库
- 除 GoalAgent 外的领域 Agent 不做详设（保持 L1 框图深度）

## 验收标准明细

1. 设计稿提交 Codex 与 Gemini 各完成一轮评审，意见按 harness/README.md §5 回流为 BUG/REQ/ADR Review Notes
2. 所有 `bug_type: req_bug` 且 severity ≥ high 的缺陷关闭
3. human-001 在每个 proposed 状态 ADR 上完成裁决（accepted 或打回重写）
4. **ADR 状态一致性检查（BUG-006 gate）**：每个 `status: accepted` 的 ADR 满足 adr-standard §4.1 评审闭环 gate——`informed_by` 完整、Review Notes 无悬空未裁意见、关联 BUG 全闭、决策变更已同步正文；存在未闭环 accepted ADR 则本 REQ 不得置 done

## 设计引用

- docs/design/00-overview.html（文档地图）
- harness/adr-standard.md（评审意见追加规则）

## Bug History

（暂无）
