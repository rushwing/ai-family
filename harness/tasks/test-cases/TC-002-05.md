---
tc_id: TC-002-05
linked_req: REQ-002
title: "PG RLS 绕过路径全封堵 + 平台状态表同隔离（critical）"
status: draft
level: integration
owner: claude-code-001
automated: true
---

## 前置条件
- 同 TC-002-04；并配置受限迁移 role、worker role、（如用）PgBouncer transaction 模式
- 平台状态表已建：langgraph checkpoint / outbox / audit / confirm_token / tool call log

## 步骤（逐绕过路径，BUG-002 / BUG-014）
1. 以应用 role 验证其 **无 BYPASSRLS**；业务表均 `FORCE ROW LEVEL SECURITY`
2. 经 `SECURITY DEFINER` 函数尝试跨成员读取
3. 后台 worker / 图节点以默认（未设 member/shared scope）尝试读写
4. PgBouncer transaction 模式下，连接复用：A 的事务结束后 B 复用同物理连接读取
5. 迁移/DDL role 尝试读业务数据
6. 对**平台状态表**（checkpoint/outbox/audit/confirm_token/tool log）做跨成员读取
7. 运行 CI RLS 扫描：新增一张无 RLS 策略的表

## 期望结果
- 1–6：以上每条路径跨成员读取均返回 **0 行 / 被拒**（FORCE RLS 生效、SECURITY DEFINER 默认禁、worker 必设 scope、事务内 SET LOCAL per-txn claim 防串号）
- 平台状态表与业务表同等隔离
- 7：CI RLS 扫描对无策略新表 **判失败**

## 实现位置
- `tests/integration/test_rls_bypass.py`（pytest 矩阵 + PG/PgBouncer testcontainer）
- CI 扫描：`tools/` 下 RLS 扫描脚本（REQ-002 实现期落地）
- 回归：BUG-002（critical）/ BUG-014（high）
