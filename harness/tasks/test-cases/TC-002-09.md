---
tc_id: TC-002-09
linked_req: REQ-002
title: "Langfuse：一次 LLM 调用产生完整 trace"
status: implemented
level: integration
owner: claude-code-001
automated: true
---

## 前置条件
- Langfuse 起于 NAS；LiteLLM 出口接入 Langfuse

## 步骤
1. 携带 `trace_id` 发起一次经 LiteLLM 的 LLM 调用
2. 在 Langfuse 按 trace_id 检索
3. 校验该 trace 含 token / 成本 / 延迟字段
4. 校验 trace_id 从入口贯通到 LLM span

## 期望结果（对应 「Langfuse / 一次 LLM 调用产生完整 trace（token/成本/延迟）」）
- trace 可检索且字段完整（token/cost/latency）
- trace_id 端到端贯通

## 实现位置
- `tests/integration/test_langfuse_trace.py`（pytest + Langfuse dev 实例 / SDK）

## 本地 E2E 验证（2026-06-20，OrbStack）
- ✅ 已验：Langfuse /api/public/health 200（服务可达，DB 迁移完成）。
- ⏳ 待补：一次经 LiteLLM 的调用 → 按 trace_id 检索 → 断言 token/成本/延迟字段完整 + trace_id 贯通。状态保持 implemented。
