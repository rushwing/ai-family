---
tc_id: TC-002-08
linked_req: REQ-002
title: "LiteLLM 网关（REQ-002 层）：厂商 key 隔离 + 三后端路由 dispatch"
status: passing
level: integration
owner: generator-001
automated: true
---

## 范围（REQ-002 = 网关本身的机制）
本 TC 只验**网关层**可在无真实厂商 key 下确证的行为。**真实 provider completion、per-user 预算计数/超限拒绝、缺/错 user 拒绝**依赖真实凭证 + agent-core 的 per-user 身份，已拆至 **REQ-003 / BUG-030**（见下"移出项"）。

## 前置条件
- LiteLLM 起栈（compose.dev.yaml），config 配 DeepSeek/Kimi/Claude 三档 + master key；dev 无真实厂商 key

## 步骤
1. 不带网关 key 请求 `/v1/models`
2. 带 master key 请求 `/v1/models`
3. 带 key 分别对 `deepseek-chat`/`kimi`/`claude` 发 completion
4. 带 key 对未配置 model 发 completion

## 期望结果（网关层）
1. 无 key → 401/403（厂商 key 仅在网关，下游无网关 key 不可调用 —— BUG-003 隔离）
2. 带 key → 列出 deepseek-chat / kimi / claude 三档
3. 三档各路由到**对应 provider**：dev 无真实厂商 key → 返回该 provider 专属鉴权错（DeepseekException / MoonshotException / AnthropicException），证明 model_name 被解析并 dispatch 到正确后端（而非 "model not found"）
4. 未配置 model → 网关拒为 `Invalid model name`（未路由）

## 移出项（→ REQ-003 / BUG-030，真实凭证 + agent 侧）
- 真实三路 completion（连 DeepSeek/Kimi/Claude 出结果）
- per-user 预算计数 / 超限拒绝（LiteLLM 虚拟 key + DB 计量）
- 缺/错 user（虚拟 key）拒绝

## 实现位置
- `tests/integration/test_services.py::test_litellm_key_isolation_and_dispatch`
- 回归：BUG-003（网关侧 key 隔离）

## 验证（2026-06-20，OrbStack，本地真跑 passing）
- ✅ 无 key 401；列三档；deepseek-chat→deepseek / kimi→moonshot / claude→anthropic 路由确认；bogus→Invalid model name
