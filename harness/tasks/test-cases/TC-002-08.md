---
tc_id: TC-002-08
linked_req: REQ-002
title: "LiteLLM 网关：三后端路由 + per-user 预算计数 + 厂商 key 隔离"
status: implemented
level: integration
owner: claude-code-001
automated: true
---

## 前置条件
- LiteLLM 起于运行时节点；配置 DeepSeek / Kimi / Claude 三后端与 per-user 预算

## 步骤
1. 分别请求三个模型档位，确认路由到对应后端
2. 以某测试用户连续调用，验证 per-user 预算计数累加
3. 超预算后再调用
4. 校验**厂商 API key 仅在网关侧持有**：下游 Agent/调用方拿不到原始 key（BUG-003）
5. 用错误/缺失 user 标识调用

## 期望结果（对应 「LiteLLM / 路由三后端，per-user 预算计数」+ BUG-003）
- 三后端均可路由成功
- 预算计数按 user 正确累加；超预算被拦/降级
- 下游无法获取厂商 key（key 隔离有验证机制，非"假设隔离"）
- 缺 user 标识被拒（不计入他人预算）

## 实现位置
- `tests/integration/test_litellm_gateway.py`（pytest，mock 后端或 dev 实例）
- 回归：BUG-003（high，LiteLLM 厂商 key 隔离验证）
