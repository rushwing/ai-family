---
tc_id: TC-002-06
linked_req: REQ-002
title: "Redis 会话缓存可用"
status: draft
level: integration
owner: claude-code-001
automated: true
---

## 前置条件
- Redis 起于运行时节点（618 前临时 Pi5），仅 tailnet 内可达

## 步骤
1. 写入一个会话键（带 TTL）
2. 读取并校验值与 TTL
3. TTL 到期后再读
4. 从非 tailnet 地址尝试连接

## 期望结果（对应 「Redis / 会话缓存可用」）
- 读写正常；TTL 生效（过期后 miss）
- 非 tailnet 不可达（仅监听 tailnet 地址）

## 实现位置
- `tests/integration/test_redis.py`（pytest + redis testcontainer / dev 实例）
