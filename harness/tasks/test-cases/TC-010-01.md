---
tc_id: TC-010-01
linked_req: REQ-010
title: "Tailscale mesh 四节点互通且 ACL 生效"
status: implemented
level: integration
owner: claude-code-001
automated: false
---

## 前置条件
- Pi5 / Mac Mini / NAS / VPS 均已加入 tailnet（618 前 Mac Mini 缺位则按 Pi5+NAS 双节点降级）
- ACL 策略文件已应用（tag:infra / tag:runtime / tag:ingress / tag:admin / tag:member / tag:kid）

## 步骤
1. 各节点间 `tailscale ping` 验证可达
2. 从 `tag:ingress`(Pi5) 尝试连 `tag:runtime`(Mac Mini) 的 `:8000`，再尝试连 NAS 的 `:5432`
3. 从 `tag:kid` 设备尝试访问任意 tag:infra 端口
4. 从 `tag:admin` SSH 到 tag:infra（check 模式）

## 期望结果（对应 REQ-002 「Tailscale mesh / 四节点互通，ACL 生效」）
- 节点互通 OK
- ingress→runtime:8000 通；ingress→NAS:5432 **被拒**（dst 限 tag:runtime:8000-8099，BUG-008/02 §3）
- kid 设备无任何 tailnet 访问（默认拒绝）
- admin SSH check 通过

## 实现位置
- 探测脚本：`infra/tests/tailscale_acl_check.sh`（tailnet 内手工/CI runner 执行）
- ACL 真值：`infra/tailscale/acl.json`（对齐 docs/design/02 §3）
