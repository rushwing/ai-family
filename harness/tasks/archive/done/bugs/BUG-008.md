---
bug_id: BUG-008
title: "02 部署图缺 Pi5→Mac Mini/K3s 路由与 Tailscale ACL，图文不一致"
bug_type: req_bug
status: resolved
severity: medium
owner: unassigned
linked_req: REQ-002
regression_tc: []
blocked_reason: ""
---

## 现象
02 SVG 显示 Cloudflare Tunnel 直连树莓派(Ingress)，但未画出树莓派如何将流量安全路由到 Mac Mini 的运行时/K3s 集群。图文不一致掩盖了网络配置复杂性；若用 Tailscale Subnet Router 未明确 ACL，树莓派被攻破将导致内网全穿透。

## 复现步骤 / 评审原文
- [gemini-005][2026-06-14] 在 SVG 中补全 Tailscale 网段和 K3s Ingress Controller 的交互连线，并在正文补充 Tailscale ACL 配置。

## 期望行为
部署图与正文一致呈现 ingress→运行时的路由路径与 ACL 边界。

## 根因分析
02 重画时聚焦节点拓扑，未细化 ingress 到运行时的流量路由与 subnet-router 安全面。

## 修复方案
02 §1 SVG 补 Pi5↔Mac Mini 路由连线与 Tailscale 网段标注；正文/ADR-007 补 subnet-router ACL（最小化 Pi5 可达范围）。

## 修复记录
- [claude-code-001][2026-06-14] 已落地三处：① 02 §1 SVG 新增 tailnet 路由箭头（marker b3）与图例带——① chatui(Pi5)→agent-core(Mac Mini):8000、② agent-core/ingest-worker→PG(NAS):5432·neo4j:7687·minio:9000，并标注「Pi5 非 Subnet Router」；② 02 §3 ACL 块把 Pi5 从 `tag:infra` 拆为 `tag:ingress`，新增 ingress→runtime `:8000-8099` 端口收敛规则，并加 danger-box 说明 Subnet Router 风险与 blast radius 收敛、M5 K3s NetworkPolicy 承接；③ ADR-007 Decision 补 ingress 最小化/非 Subnet Router 条目，Review Notes 标注闭环。状态 → resolved，待 human-001 合并关闭。
