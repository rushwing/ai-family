---
bug_id: BUG-009
title: "06 GoalAgent 状态图未标 FastMCP 鉴权边界，图中数据流绕过网关"
bug_type: req_bug
status: closed
severity: medium
owner: unassigned
linked_req: REQ-003
regression_tc: []
blocked_reason: ""
---

## 现象
06 GoalAgent 状态图的 “Tool Execution” 节点未标出 FastMCP 鉴权边界；图中数据流直接绕过鉴权网关，与正文“工具侧鉴权”（ADR-010）及 02 信任边界 3 不一致。

## 复现步骤 / 评审原文
- [gemini-r2][2026-06-14] 06 GoalAgent 状态图：图中画了 "Tool Execution" 节点，但未标明 FastMCP 的鉴权边界；正文提到“工具侧鉴权”，但图中数据流直接绕过了鉴权网关。

## 期望行为
状态图显式画出 MCP 网关 / 工具侧鉴权边界，数据流经网关（验签 + 白名单 + 工具侧 JWT）。

## 根因分析
06 图聚焦编排流，省略了安全边界，造成图文不符。

## 修复方案
06 SVG 在 Tool Execution 前插入 MCP 网关节点（网关验签 + 白名单 + 工具侧 JWT，REQ-004），与 02 信任边界 3 对齐。

## 修复记录
- [claude-code-001][2026-06-14] 在细化 BUG-006 gate 时连带识别：本 BUG 性质同 BUG-008（图文不符的决策一致性缺陷，关联 ADR-010），按 gate §4.1 阻塞 ADR-010 的 accepted，故一并闭环。06 §2 状态图 SVG 新增「🔒 工具侧鉴权边界」带（marker g3）：显式画出 `写操作节点 → MCP 网关（验签/白名单/工具侧 JWT）→ PG(RLS)/MinIO` 三段流，并注明 commit_plan/daily_plan/evaluate 等所有 goal-mcp 工具调用统一经网关、图中无绕过网关的数据流，对齐 ADR-010 信任边界 3 与 02 §3。SVG 高度 430→540、底部说明带下移。状态 → resolved，待 human-001 合并关闭。
